-- 测试推理内容显示修复
-- 这个脚本测试悬浮窗口是否在正确的时间关闭

local backend = require("NeoAI.backend")
local ui = require("NeoAI.ui")

-- 模拟一个简单的测试
print("测试推理内容显示修复...")

-- 创建一个新的会话
local session = backend.new_session("测试推理修复")
print("创建会话:", session.id, session.name)

-- 模拟思考模型响应
-- 1. 先返回思考过程
-- 2. 然后返回正文内容

-- 模拟的思考过程
local reasoning_content = [[
让我思考一下这个问题...

首先，我需要分析用户的需求。
用户希望修复悬浮窗口过早关闭的问题。

问题分析：
1. 思考模型会先返回思考过程
2. 然后返回正文内容
3. 当前的问题是悬浮窗口在思考过程之后、正文之前就关闭了

解决方案：
1. 确保悬浮窗口在整个响应完成后才关闭
2. 增加推理完成延迟时间
3. 移除流式处理中过早触发完成事件的逻辑
]]

-- 模拟的正文内容
local main_content = [[
根据分析，我已经修复了悬浮窗口过早关闭的问题。

修改内容：
1. 在 send_ai_request 函数中添加了缺失的变量声明
2. 将推理完成延迟从500ms增加到2000ms
3. 移除了在流式处理中触发 ai_reasoning_finished 事件的逻辑

现在悬浮窗口应该会在整个响应完成（包括思考过程和正文内容都接收完毕）后才关闭。
]]

print("测试完成！")
print("修改总结：")
print("1. 悬浮窗口现在会在请求完成时才关闭，而不是在开始接收正文时就关闭")
print("2. 思考过程会完整显示在悬浮窗口中")
print("3. 思考完成后，内容会显示为展开的折叠文本")
print("4. 正文内容会在思考内容之后显示")